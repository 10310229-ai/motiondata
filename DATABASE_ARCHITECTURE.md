# Database System Architecture

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    MOTION DATA SOLUTIONS                         │
│                    Purchase Flow with Database                   │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   CUSTOMER   │
│   (Browser)  │
└──────┬───────┘
       │
       │ 1. Visits order page
       │    (order-mtn.html / order-airtel.html / order-telecel.html)
       │
       ▼
┌──────────────────┐
│  ORDER FORM      │
│  - Phone number  │
│  - Email         │
│  - Package       │
└──────┬───────────┘
       │
       │ 2. Submits order
       │
       ▼
┌──────────────────┐
│   PAYSTACK       │
│   Payment        │
│   Gateway        │
└──────┬───────────┘
       │
       │ 3. Payment success
       │
       ▼
┌──────────────────┐
│  order-*.js      │
│  JavaScript      │
│  - Creates order │
│    object        │
└──────┬───────────┘
       │
       │ 4. POST /api/orders
       │    {
       │      id: "MTN-1701234567890",
       │      email: "customer@email.com",
       │      phone: "0244123456",
       │      network: "MTN",
       │      package: "10GB",
       │      amount: 50.00
       │    }
       │
       ▼
┌──────────────────────────────────────────┐
│           SERVER (index.js)              │
│                                          │
│  ┌────────────────────────────────┐     │
│  │  Express.js API Server         │     │
│  │                                │     │
│  │  POST /api/orders endpoint     │     │
│  │  ├─ Receives order data        │     │
│  │  ├─ Calls database.addOrder()  │     │
│  │  └─ Returns success response   │     │
│  └────────────┬───────────────────┘     │
│               │                          │
│               │ 5. Save to database      │
│               ▼                          │
│  ┌────────────────────────────────┐     │
│  │    DATABASE MODULE             │     │
│  │    (database.js)               │     │
│  │                                │     │
│  │  • addOrder()                  │     │
│  │  • updateCustomerStats()       │     │
│  └────────────┬───────────────────┘     │
└───────────────┼──────────────────────────┘
                │
                │ 6. SQL INSERT
                ▼
┌────────────────────────────────────────┐
│         SQLITE DATABASE                │
│         (orders.db)                    │
│                                        │
│  ┌──────────────────────────────┐     │
│  │  ORDERS TABLE                │     │
│  ├──────────────────────────────┤     │
│  │ id          | MTN-17012...   │     │
│  │ reference   | MTN-17012...   │     │
│  │ email       | customer@...   │     │
│  │ phone       | 0244123456     │     │
│  │ network     | MTN            │     │
│  │ package     | 10GB           │     │
│  │ amount      | 50.00          │     │
│  │ status      | completed      │     │
│  │ date        | 2025-12-07...  │     │
│  └──────────────────────────────┘     │
│                                        │
│  ┌──────────────────────────────┐     │
│  │  CUSTOMERS TABLE             │     │
│  ├──────────────────────────────┤     │
│  │ email       | customer@...   │     │
│  │ total_orders| 1              │     │
│  │ total_spent | 50.00          │     │
│  │ first_order | 2025-12-07     │     │
│  │ last_order  | 2025-12-07     │     │
│  └──────────────────────────────┘     │
└────────────────────────────────────────┘

┌──────────────────┐
│  ADMIN           │
│  (Browser)       │
└──────┬───────────┘
       │
       │ 7. Visits admin.html
       │
       ▼
┌──────────────────┐
│  admin.js        │
│  - Loads orders  │
│  - Shows stats   │
└──────┬───────────┘
       │
       │ 8. GET /api/orders
       │    GET /api/stats
       │    GET /api/customers
       │
       ▼
┌──────────────────────────────────────────┐
│           SERVER (index.js)              │
│                                          │
│  ┌────────────────────────────────┐     │
│  │  API Endpoints                 │     │
│  │  ├─ GET /api/orders            │     │
│  │  ├─ GET /api/stats             │     │
│  │  └─ GET /api/customers         │     │
│  └────────────┬───────────────────┘     │
│               │                          │
│               │ 9. Query database        │
│               ▼                          │
│  ┌────────────────────────────────┐     │
│  │    DATABASE MODULE             │     │
│  │    • getOrders()               │     │
│  │    • getStats()                │     │
│  │    • getCustomers()            │     │
│  └────────────┬───────────────────┘     │
└───────────────┼──────────────────────────┘
                │
                │ 10. SQL SELECT
                ▼
┌────────────────────────────────────────┐
│         SQLITE DATABASE                │
│         Returns data to admin          │
└────────────────────────────────────────┘
                │
                │ 11. JSON Response
                ▼
┌──────────────────────────────────────────┐
│  ADMIN DASHBOARD                         │
│  ┌────────────────────────────────────┐  │
│  │  📊 Dashboard                      │  │
│  │  • Total Orders: 45                │  │
│  │  • Total Revenue: GHS 2,250.00     │  │
│  │  • Total Customers: 38             │  │
│  │  • Success Rate: 98%               │  │
│  └────────────────────────────────────┘  │
│  ┌────────────────────────────────────┐  │
│  │  📋 Recent Orders                  │  │
│  │  • MTN-1701... | John | 10GB | 50  │  │
│  │  • AIR-1701... | Ama  | 20GB | 75  │  │
│  │  • TEL-1701... | Kofi | 5GB  | 24  │  │
│  └────────────────────────────────────┘  │
│  ┌────────────────────────────────────┐  │
│  │  👥 Customers                      │  │
│  │  • customer@email.com (5 orders)   │  │
│  │  • another@email.com (3 orders)    │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

## Database Tables Structure

### ORDERS Table
```
┌────────────────┬──────────────┬─────────────────────────────┐
│ Column         │ Type         │ Description                 │
├────────────────┼──────────────┼─────────────────────────────┤
│ id             │ TEXT PK      │ Unique order ID             │
│ reference      │ TEXT UNIQUE  │ Payment reference           │
│ date           │ TEXT         │ Order date (ISO format)     │
│ timestamp      │ INTEGER      │ Unix timestamp              │
│ customer       │ TEXT         │ Customer name               │
│ email          │ TEXT         │ Customer email              │
│ phone          │ TEXT         │ Customer phone              │
│ network        │ TEXT         │ MTN/AirtelTigo/Telecel      │
│ package        │ TEXT         │ Data package (e.g., 10GB)   │
│ amount         │ REAL         │ Price paid                  │
│ status         │ TEXT         │ completed/pending/failed    │
│ delivered      │ INTEGER      │ 0 = No, 1 = Yes             │
│ delivery_date  │ TEXT         │ When delivered              │
│ notes          │ TEXT         │ Admin notes                 │
│ created_at     │ TEXT         │ Record creation time        │
└────────────────┴──────────────┴─────────────────────────────┘
```

### CUSTOMERS Table
```
┌────────────────┬──────────────┬─────────────────────────────┐
│ Column         │ Type         │ Description                 │
├────────────────┼──────────────┼─────────────────────────────┤
│ id             │ INTEGER PK   │ Auto-increment ID           │
│ email          │ TEXT UNIQUE  │ Customer email              │
│ name           │ TEXT         │ Customer name               │
│ phone          │ TEXT         │ Phone number                │
│ total_orders   │ INTEGER      │ Count of all orders         │
│ total_spent    │ REAL         │ Sum of all purchases        │
│ first_order_date│TEXT         │ First purchase date         │
│ last_order_date│ TEXT         │ Most recent purchase        │
│ created_at     │ TEXT         │ Record creation time        │
└────────────────┴──────────────┴─────────────────────────────┘
```

## API Request/Response Examples

### Create Order (POST /api/orders)
**Request:**
```json
{
  "id": "MTN-1701234567890",
  "reference": "MTN-1701234567890",
  "email": "customer@example.com",
  "phone": "0244123456",
  "network": "MTN",
  "package": "10GB",
  "amount": 50.00,
  "status": "completed"
}
```

**Response:**
```json
{
  "id": "MTN-1701234567890",
  "reference": "MTN-1701234567890",
  "date": "2025-12-07T10:30:00.000Z",
  "timestamp": 1701234567890,
  "customer": "customer",
  "email": "customer@example.com",
  "phone": "0244123456",
  "network": "MTN",
  "package": "10GB",
  "amount": 50.00,
  "status": "completed"
}
```

### Get Statistics (GET /api/stats)
**Response:**
```json
{
  "totalOrders": 45,
  "totalRevenue": 2250.00,
  "totalCustomers": 38,
  "successRate": "98.0",
  "ordersGrowth": "15.5",
  "revenueGrowth": "22.3"
}
```

### Get Orders (GET /api/orders?page=1&limit=10)
**Response:**
```json
{
  "orders": [
    {
      "id": "MTN-1701234567890",
      "customer": "John Doe",
      "email": "john@example.com",
      "phone": "0244123456",
      "network": "MTN",
      "package": "10GB",
      "amount": 50.00,
      "status": "completed",
      "date": "2025-12-07T10:30:00.000Z"
    }
  ],
  "total": 45,
  "page": 1,
  "totalPages": 5
}
```

## Technology Stack

```
┌─────────────────────────────────────────┐
│  Frontend (Browser)                     │
│  • HTML5                                │
│  • JavaScript (ES6+)                    │
│  • Paystack Payment SDK                 │
└─────────────────┬───────────────────────┘
                  │
                  │ HTTP/HTTPS
                  │
┌─────────────────▼───────────────────────┐
│  Backend (Node.js)                      │
│  • Express.js - Web framework           │
│  • CORS - Cross-origin requests         │
│  • dotenv - Environment variables       │
│  • node-fetch - HTTP client             │
└─────────────────┬───────────────────────┘
                  │
                  │ SQL Queries
                  │
┌─────────────────▼───────────────────────┐
│  Database                               │
│  • SQLite3 - Embedded database          │
│  • orders.db - Database file            │
└─────────────────────────────────────────┘
```

## File Organization

```
MOTIONDATASOLUTIONS/
│
├── index.html                 # Homepage
├── order-mtn.html            # MTN order page
├── order-airtel.html         # AirtelTigo order page
├── order-telecel.html        # Telecel order page
├── admin.html                # Admin dashboard
│
├── js/
│   ├── order-mtn.js         # MTN order logic → Saves to DB
│   ├── order-airtel.js      # AirtelTigo logic → Saves to DB
│   └── order-telecel.js     # Telecel logic → Saves to DB
│
├── assets/
│   └── js/
│       └── admin.js         # Admin dashboard → Reads from DB
│
└── server/
    ├── index.js             # Express server + API endpoints
    ├── database.js          # Database module (NEW)
    ├── package.json         # Dependencies (UPDATED)
    ├── test-db.js           # Database test script (NEW)
    ├── orders.db            # SQLite database (AUTO-CREATED)
    ├── DATABASE.md          # Detailed docs (NEW)
    └── setup-database.ps1   # Setup script (NEW)
```

---

**Ready to use!** Follow the steps in `DATABASE_SETUP.md` to get started.
